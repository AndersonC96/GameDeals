RewriteEngine On

# Allow direct access to init_db.php
RewriteRule ^init_db\.php$ - [L]

# Redirect all other requests to public folder
RewriteCond %{REQUEST_URI} !^/GameDeals/public/
RewriteRule ^(.*)$ public/$1 [L]
